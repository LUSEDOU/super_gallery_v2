name: super_gallery_v2
description: A Very Good Project created by Very Good CLI.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: 3.0.4

dependencies:
  bloc: ^8.1.0
  cached_network_image: ^3.2.1
  card_swiper: ^2.0.4
  equatable: ^2.0.3
  flow_builder: ^0.0.9
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  flutter_localizations:
    sdk: flutter
  formz_input:
    path: packages/formz_input
  images_api:
    path: packages/images_api
  images_repository:
    path: packages/images_repository
  intl: ^0.17.0
  local_storage_images_api:
    path: packages/local_storage_images_api
  pinch_zoom: ^1.0.0
  stream_transform: ^2.0.0

dev_dependencies:
  bloc_test: ^9.0.3
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.0.1

flutter:
  uses-material-design: true
  generate: true
